import { backendDB } from "data/backendDB/BackendDB";

export default describe("Tests the database", () => {
  test("Test", () => {
    //expect(backendDB.isUserAdmin("jorgestar29")).resolves.toEqual(true);
  });
});

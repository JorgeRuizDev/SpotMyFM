import React from "react";
import CompleteStats from "./CompleteStats";
import { render } from "@testing-library/react";

describe("<CompleteStats />", () => {
  test("Renders the component", () => {
    const component = render(<CompleteStats />);
  });
});

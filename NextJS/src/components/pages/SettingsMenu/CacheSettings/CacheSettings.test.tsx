import React from "react";
import CacheSettings from "./CacheSettings";
import { render } from "@testing-library/react";

describe("<CacheSettings />", () => {
  test("Renders the component", () => {
    const component = render(<CacheSettings />);
  });
});

import React from "react";
import NavDropDown from "./NavDropDown";
import { render } from "@testing-library/react";

describe("<NavDropDown />", () => {
  test("Renders the component", () => {
    const component = render(<NavDropDown />);
  });
});

import React from "react";
import ViewHeading from "./ViewHeading";
import { render } from "@testing-library/react";

describe("<ViewHeading />", () => {
  test("Renders the component", () => {
    const component = render(<ViewHeading />);
  });
});

import React from "react";
import SelectAllButton from "./SelectAllButton";
import { render } from "@testing-library/react";

describe("<SelectAllButton />", () => {
  test("Renders the component", () => {
    const component = render(<SelectAllButton />);
  });
});
